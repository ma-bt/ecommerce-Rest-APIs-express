let multer,path,CustomErrorHandler;_4dc‍.x([["default",()=>_4dc‍.o]]);_4dc‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_4dc‍.w("path",[["default",["path"],function(v){path=v}]]);_4dc‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);



const storage = multer.diskStorage({
  destination: (req, file, cb) => cb(null, "uploads/"),
  filename: (req, file, cb) => {
    const uniqueName = `${Date.now()}- ${Math.round(
      Math.random() * 1e9
    )}${path.extname(file.originalname)}`; //formula to generate unique name

    cb(null, uniqueName);
  },
});

const handleMultipartData = multer({
  storage,
  limits: { fileSize: 1000000 * 5 },
}).single("image");

const productController = {
  async store(req, res, next) {
    /* Multipart form data */

    handleMultipartData(req, res, (err) => {
      if (err) {
        return next(CustomErrorHandler.serverError(err.message));
      }
      _4dc‍.g.console.log(req.file);

      res.json({})

    //   const filePath = req.file.path;
    });
  },
};
_4dc‍.d(productController);
