let multer,path,CustomErrorHandler;_3dc‍.x([["default",()=>_3dc‍.o]]);_3dc‍.w("multer",[["default",["multer"],function(v){multer=v}]]);_3dc‍.w("path",[["default",["path"],function(v){path=v}]]);_3dc‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);



const storage = multer.diskStorage({
  destination: (req, res, cb) => cb(null, "uploads/"),
  filename: (req, res, cb) => {
    const uniqueName = `${Date.now()}- ${Math.round(
      Math.random() * 1e9
    )}${path.extname(file.originalname)}`; //formula to generate unique name

    cb(null, uniqueName);
  },
});

const handleMultipartData = multer({
  storage,
  limits: { fileSize: 1000000 * 5 },
}).single("image");

const productController = {
  async store(req, res, next) {
    /* Multipart form data */

    handleMultipartData(req, res, (err) => {
      if (err) {
        return next(CustomErrorHandler.serverError(err.message));
      }
      _3dc‍.g.console.log(req.file);

      res.json({})

    //   const filePath = req.file.path;
    });
  },
};
_3dc‍.d(productController);
